# KnowledgePower 详细设计文档 v1.0

> 对应项目规划方案 Phase 1：骨架搭建
> 最新更新：2026-07-10

---

## 目录

1. [环境与依赖](#1-环境与依赖)
2. [项目目录结构](#2-项目目录结构)
3. [数据库设计](#3-数据库设计)
4. [后端 API 设计](#4-后端-api-设计)
5. [前端组件体系](#5-前端组件体系)
6. [知识图谱数据模型](#6-知识图谱数据模型)
7. ["数的世界"领域内容设计](#7-数的世界领域内容设计)
8. [互动演示设计](#8-互动演示设计)
9. [Phase 1 实现任务拆解](#9-phase-1-实现任务拆解)

---

## 1. 环境与依赖

### 1.1 开发环境

| 项目 | 版本 | 说明 |
|------|------|------|
| JDK | 21 | 后端 |
| Maven | 3.9+ | 构建 |
| Node.js | 20+ | 前端 |
| MySQL | 8.0+ | 数据库 |
| Redis | 7.x | 缓存 |
| IDE | IntelliJ IDEA / VS Code | |

### 1.2 后端依赖（Spring Boot 3.x）

```xml
<!-- pom.xml 核心依赖 -->
Spring Boot 3.4.x
  ├── spring-boot-starter-web       # REST API
  ├── spring-boot-starter-validation # 参数校验
  ├── mybatis-plus-spring-boot3-starter  # ORM
  ├── mysql-connector-j             # MySQL 驱动
  ├── spring-boot-starter-data-redis     # Redis
  ├── springdoc-openapi-starter-webmvc-ui # API 文档 (Swagger)
  ├── spring-boot-starter-test      # 测试
  ├── h2 (test scope)               # 测试用内存数据库
  ├── hutool-all                    # 工具类
  ├── lombok                        # 实体类简化
  └── fastjson2 / jackson           # JSON 处理
```

### 1.3 前端依赖

```json
{
  "dependencies": {
    "react": "^18",
    "react-router-dom": "^6",
    "axios": "^1",
    "d3": "^7",
    "three": "^0.160",
    "katex": "^0.16",
    "react-katex": "^3",
    "zustand": "^4",
    "antd": "^5",
    "@ant-design/icons": "^5",
    "dayjs": "^1"
  },
  "devDependencies": {
    "typescript": "^5",
    "vite": "^5",
    "@vitejs/plugin-react": "^4",
    "less": "^4",
    "vitest": "^1",
    "@testing-library/react": "^14"
  }
}
```

### 1.4 环境配置

按照你的规范（`application-dev.yml` / `application-test.yml` / `application-prod.yml`），默认 dev。

---

## 2. 项目目录结构

```
math-knowledge/
│
├── doc/                                # 文档
│   ├── 项目规划方案.md
│   └── 详细设计文档.md                  # ← 本文件
│
├── knowledge-base/                     # 知识库原始数据（人工维护，作为初始化 seed）
│   ├── 01-数的世界/
│   │   ├── index.json                  # 章节索引
│   │   ├── 01-自然数.md                 # 知识点 Markdown 原文
│   │   ├── 02-整数与负数.md
│   │   ├── 03-有理数.md
│   │   ├── 04-实数.md
│   │   ├── 05-复数.md
│   │   └── summary-数的世界.md           # 总结节点
│   ├── 02-式/
│   │   └── ...
│   └── seed-data.json                  # 合并所有种子数据（用于初始化 DB）
│
├── server/                             # Spring Boot 后端 —— 单体起步，后续拆微服务
│   ├── pom.xml
│   └── src/
│       ├── main/
│       │   ├── java/com/knowledgepower/
│       │   │   ├── KnowledgePowerApplication.java
│       │   │   │
│       │   │   ├── config/
│       │   │   │   ├── WebMvcConfig.java          # CORS/拦截器
│       │   │   │   ├── MyBatisPlusConfig.java     # 分页/乐观锁
│       │   │   │   ├── RedisConfig.java
│       │   │   │   ├── Knife4jConfig.java         # API 文档
│       │   │   │   └── JacksonConfig.java         # JSON 序列化
│       │   │   │
│       │   │   ├── common/                        # 公共模块
│       │   │   │   ├── exception/
│       │   │   │   │   ├── GlobalExceptionHandler.java
│       │   │   │   │   ├── BusinessException.java
│       │   │   │   │   └── ErrorCode.java
│       │   │   │   ├── result/
│       │   │   │   │   ├── R.java                 # 统一响应体
│       │   │   │   │   └── PageResult.java
│       │   │   │   └── util/
│       │   │   │       └── DesensitizedUtil.java
│       │   │   │
│       │   │   ├── knowledge/                     # 知识管理模块 —— 核心
│       │   │   │   ├── entity/
│       │   │   │   │   ├── KnowledgeNode.java     # 知识点实体
│       │   │   │   │   ├── KnowledgeRelation.java # 知识点关系
│       │   │   │   │   └── KnowledgeContent.java  # 知识点内容（JSON）
│       │   │   │   ├── mapper/
│       │   │   │   │   ├── KnowledgeNodeMapper.java
│       │   │   │   │   └── KnowledgeRelationMapper.java
│       │   │   │   ├── service/
│       │   │   │   │   ├── KnowledgeNodeService.java
│       │   │   │   │   ├── KnowledgeNodeServiceImpl.java
│       │   │   │   │   ├── KnowledgeRelationService.java
│       │   │   │   │   ├── KnowledgeRelationServiceImpl.java
│       │   │   │   │   └── KnowledgeGraphService.java     # 图谱查询
│       │   │   │   ├── controller/
│       │   │   │   │   ├── KnowledgeNodeController.java   # 知识点 CRUD
│       │   │   │   │   ├── KnowledgeRelationController.java
│       │   │   │   │   └── KnowledgeGraphController.java  # 图谱数据
│       │   │   │   └── dto/
│       │   │   │       ├── KnowledgeNodeDTO.java
│       │   │   │       ├── KnowledgeNodeQueryDTO.java
│       │   │   │       ├── KnowledgeGraphVO.java          # 图谱视图对象
│       │   │   │       └── KnowledgeNodeDetailVO.java     # 详情页视图
│       │   │   │
│       │   │   ├── user/                        # 用户模块（Phase 1 简化）
│       │   │   │   ├── entity/User.java
│       │   │   │   ├── mapper/UserMapper.java
│       │   │   │   ├── service/UserService.java
│       │   │   │   └── controller/UserController.java
│       │   │   │
│       │   │   └── seed/                        # 种子数据
│       │   │       └── DataSeeder.java          # 启动时初始化知识库
│       │   │
│       │   └── resources/
│       │       ├── application.yml
│       │       ├── application-dev.yml
│       │       ├── application-test.yml
│       │       ├── application-prod.yml
│       │       ├── logback-spring.xml            # 日志配置
│       │       └── db/
│       │           └── init.sql                  # 建表 DDL
│       │
│       └── test/java/com/knowledgepower/
│           └── knowledge/
│               ├── KnowledgeNodeServiceTest.java
│               └── KnowledgeGraphControllerTest.java
│
├── web/                                # React 前端
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── index.html
│   └── src/
│       ├── main.tsx                    # 入口
│       ├── App.tsx                     # 路由配置
│       ├── api/                        # API 调用层
│       │   ├── request.ts              # axios 实例封装
│       │   ├── knowledge.ts            # 知识相关 API
│       │   └── user.ts                 # 用户相关 API
│       ├── pages/                      # 页面
│       │   ├── GraphPage.tsx           # 知识图谱主页
│       │   ├── KnowledgeDetail.tsx     # 知识点详情
│       │   ├── SearchPage.tsx          # 搜索
│       │   ├── ExamPage.tsx            # 考试（Phase 3）
│       │   └── admin/                  # 管理后台
│       │       ├── KnowledgeEdit.tsx    # 知识编辑
│       │       └── Dashboard.tsx        # 后台面板
│       ├── components/                 # 通用组件
│       │   ├── Layout/
│       │   │   ├── MainLayout.tsx       # 主布局
│       │   │   ├── Sidebar.tsx          # 侧边栏（领域导航）
│       │   │   └── Header.tsx
│       │   ├── Graph/                   # 知识图谱相关
│       │   │   ├── KnowledgeGraph.tsx    # D3 力导向图
│       │   │   ├── GraphNode.tsx         # 自定义节点
│       │   │   └── GraphControls.tsx     # 图谱控制（缩放/过滤）
│       │   ├── Visual/                  # 互动演示组件
│       │   │   ├── VisualContainer.tsx   # 演示容器
│       │   │   ├── number-line/          # 数轴
│       │   │   │   └── NumberLine.tsx
│       │   │   └── complex-plane/        # 复平面
│       │   │       └── ComplexPlane.tsx
│       │   ├── Card/
│       │   │   ├── KnowledgeCard.tsx     # 知识点卡片
│       │   │   └── SummaryCard.tsx       # 总结卡片
│       │   └── common/
│       │       ├── Formula.tsx           # KaTeX 公式渲染
│       │       ├── Loading.tsx
│       │       └── ErrorBoundary.tsx
│       ├── store/                       # 状态管理
│       │   ├── graphStore.ts            # 图谱状态
│       │   └── userStore.ts             # 用户状态
│       ├── hooks/                       # 自定义 Hooks
│       │   ├── useGraphData.ts
│       │   └── useKnowledgeDetail.ts
│       ├── types/                       # TypeScript 类型
│       │   ├── knowledge.ts
│       │   └── graph.ts
│       └── styles/                      # 样式
│           ├── global.css
│           ├── variables.less
│           └── graph.less
│
├── docker/                             # Docker 编排
│   ├── docker-compose.yml
│   └── Dockerfile.server
│
└── README.md
```

---

## 3. 数据库设计

### 3.1 建表 SQL

```sql
-- ============================================
-- 知识点表 (knowledge_node)
-- ============================================
CREATE TABLE knowledge_node (
    id              VARCHAR(32)     NOT NULL COMMENT '知识点编码，如 MATH-01-001',
    title           VARCHAR(200)    NOT NULL COMMENT '知识点名称，如 虚数单位 i',
    subtitle        VARCHAR(500)    DEFAULT NULL COMMENT '副标题/一句话定义',
    domain          VARCHAR(20)     NOT NULL COMMENT '所属领域：数/式/方程与不等式/函数/几何/排列组合与统计/数列与导数',
    level           VARCHAR(10)     NOT NULL COMMENT '难度层级：初中/高中',
    difficulty      TINYINT         NOT NULL DEFAULT 1 COMMENT '难度 1-5',
    sort_order      INT             NOT NULL DEFAULT 0 COMMENT '领域内排序',
    visual_type     VARCHAR(50)     DEFAULT NULL COMMENT '可视化类型：canvas/svg/three/static',
    summary         TEXT            DEFAULT NULL COMMENT '一句话精髓（显示在卡片/图谱节点）',
    content_json    JSON            DEFAULT NULL COMMENT '知识点详细内容（结构化 JSON）',
    milestone_type  VARCHAR(20)     DEFAULT NULL COMMENT '总结节点类型：section_end/domain_end/null',
    status          TINYINT         NOT NULL DEFAULT 1 COMMENT '状态：0=草稿 1=发布',
    created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    INDEX idx_domain (domain),
    INDEX idx_level (level),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点表';

-- ============================================
-- 知识点关系表 (knowledge_relation)
-- ============================================
CREATE TABLE knowledge_relation (
    id              BIGINT          AUTO_INCREMENT PRIMARY KEY,
    from_node_id    VARCHAR(32)     NOT NULL COMMENT '前驱知识点编码',
    to_node_id      VARCHAR(32)     NOT NULL COMMENT '后继知识点编码',
    relation_type   VARCHAR(20)     NOT NULL COMMENT '关系类型：prerequisite/next/reference/summary_of',
    sort_order      INT             NOT NULL DEFAULT 0 COMMENT '排序',
    description     VARCHAR(500)    DEFAULT NULL COMMENT '关系描述，如"学习复数前需要理解平方根"',
    created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_from (from_node_id),
    INDEX idx_to (to_node_id),
    INDEX idx_type (relation_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点关系表';

-- ============================================
-- 用户表 (user) — Phase 1 简化，后续扩展
-- ============================================
CREATE TABLE user (
    id              BIGINT          AUTO_INCREMENT PRIMARY KEY,
    username        VARCHAR(50)     NOT NULL UNIQUE,
    password        VARCHAR(255)    NOT NULL,
    nickname        VARCHAR(50)     DEFAULT NULL,
    avatar          VARCHAR(500)    DEFAULT NULL,
    role            VARCHAR(20)     NOT NULL DEFAULT 'user' COMMENT 'user/admin',
    status          TINYINT         NOT NULL DEFAULT 1,
    created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 3.2 content_json 结构（核心设计）

`knowledge_node.content_json` 字段存储的知识点详细内容，JSON 格式：

```json
{
  "text": "虚数单位 i 是复数系统的核心，满足 i² = -1。",
  "sections": [
    {
      "type": "definition",
      "title": "定义",
      "content": "虚数单位 i 定义为满足 i² = -1 的数。它不是实数，但可以与其他实数组合成复数 a+bi。",
      "formulas": ["i^2 = -1"]
    },
    {
      "type": "visualization",
      "title": "直观理解",
      "content": "乘以 i 相当于在复平面中逆时针旋转 90°",
      "visual": {
        "type": "canvas",
        "component": "complex-rotation",
        "config": {
          "defaultAngle": 90,
          "showGrid": true,
          "showLabels": true
        }
      }
    },
    {
      "type": "keypoints",
      "title": "核心要点",
      "items": [
        "i² = -1",
        "i³ = -i",
        "i⁴ = 1（每 4 次一个循环）",
        "i⁰ = 1"
      ]
    },
    {
      "type": "example",
      "title": "示例",
      "items": [
        {
          "question": "计算 (3i) × (2i)",
          "steps": ["(3i)(2i) = 6i²", "i² = -1", "= -6"],
          "answer": "-6"
        }
      ]
    },
    {
      "type": "analogy",
      "title": "类比理解",
      "content": "正负数类比：\n- ×1 = 正向（保持方向）\n- ×(-1) = 反向（旋转 180°）\n- ×i = 旋转 90°（打开新维度）"
    }
  ],
  "tags": ["复数", "虚数", "旋转"]
}
```

### 3.3 DTO 设计

```java
// ====== 统一响应体 ======
@Data
public class R<T> {
    private int code;
    private String msg;
    private T data;
    
    public static <T> R<T> ok(T data) { ... }
    public static <T> R<T> fail(String msg) { ... }
}

// ====== 知识点详情视图对象 ======
@Data
public class KnowledgeNodeDetailVO {
    private String id;              // MATH-01-001
    private String title;           // 虚数单位 i
    private String subtitle;        // 一句话定义
    private String domain;
    private String level;
    private Integer difficulty;
    private String summary;
    private String visualType;
    private JsonNode contentJson;   // 结构化内容
    
    // 关系
    private List<RelationVO> prerequisites;   // 前置知识
    private List<RelationVO> nextNodes;       // 后继知识
    private List<RelationVO> references;      // 参考关联
}

@Data
public class RelationVO {
    private String nodeId;
    private String title;
    private String relationType;
    private String description;
}

// ====== 图谱视图对象 ======
@Data
public class KnowledgeGraphVO {
    private List<GraphNodeVO> nodes;
    private List<GraphEdgeVO> edges;
}

@Data
public class GraphNodeVO {
    private String id;
    private String label;           // 显示标签
    private String domain;          // 领域（用于颜色）
    private String level;           // 层级
    private Integer difficulty;     // 用于节点大小
    private Boolean isMilestone;    // 是否是总结节点
}

@Data
public class GraphEdgeVO {
    private String source;          // from_node_id
    private String target;          // to_node_id
    private String type;            // relation_type
    private String description;
}
```

---

## 4. 后端 API 设计

### 4.1 知识模块 API

| 方法 | 路径 | 说明 | Phase |
|------|------|------|-------|
| GET | `/knowledge/nodes` | 知识点列表（分页+筛选） | P1 |
| GET | `/knowledge/nodes/{id}` | 知识点详情（含前后置关系） | P1 |
| POST | `/knowledge/nodes` | 新增知识点 | P1 |
| PUT | `/knowledge/nodes/{id}` | 更新知识点 | P1 |
| DELETE | `/knowledge/nodes/{id}` | 删除知识点 | P1 |
| GET | `/knowledge/graph` | 获取全量图谱数据（节点+边） | P1 |
| GET | `/knowledge/graph/{domain}` | 按领域获取子图谱 | P1 |
| GET | `/knowledge/graph/path?from=...&to=...` | 两点间学习路径 | P2 |
| POST | `/knowledge/relations` | 新增知识点关系 | P1 |
| DELETE | `/knowledge/relations/{id}` | 删除关系 | P1 |
| GET | `/knowledge/search?q=...` | 搜索知识点 | P1 |

### 4.2 API 详细定义

#### 4.2.1 GET /knowledge/graph — 获取全量图谱数据

**Response:**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "nodes": [
      {
        "id": "MATH-01-001",
        "label": "自然数",
        "domain": "数",
        "level": "初中",
        "difficulty": 1,
        "isMilestone": false
      },
      {
        "id": "MATH-01-002",
        "label": "整数与负数",
        "domain": "数",
        "level": "初中",
        "difficulty": 1,
        "isMilestone": false
      }
    ],
    "edges": [
      {
        "source": "MATH-01-001",
        "target": "MATH-01-002",
        "type": "prerequisite",
        "description": "自然数是理解负数的基础"
      }
    ]
  }
}
```

#### 4.2.2 GET /knowledge/nodes/{id} — 知识点详情

**Response:**

```json
{
  "code": 200,
  "data": {
    "id": "MATH-01-005",
    "title": "虚数单位 i",
    "subtitle": "i² = -1",
    "domain": "数",
    "level": "高中",
    "difficulty": 3,
    "visualType": "canvas",
    "summary": "乘以 i 相当于在复平面逆时针旋转 90°",
    "contentJson": { ... },
    "prerequisites": [
      { "nodeId": "MATH-01-004", "title": "实数", "relationType": "prerequisite" },
      { "nodeId": "MATH-04-001", "title": "平方根", "relationType": "prerequisite" }
    ],
    "nextNodes": [
      { "nodeId": "MATH-01-006", "title": "复数运算", "relationType": "next" },
      { "nodeId": "MATH-01-007", "title": "复数的三角形式", "relationType": "next" }
    ]
  }
}
```

#### 4.2.3 POST /knowledge/nodes — 新增知识点

**Request:**

```json
{
  "id": "MATH-01-005",
  "title": "虚数单位 i",
  "subtitle": "i² = -1",
  "domain": "数",
  "level": "高中",
  "difficulty": 3,
  "visualType": "canvas",
  "summary": "乘以 i 相当于在复平面逆时针旋转 90°",
  "contentJson": { "text": "...", "sections": [...] }
}
```

---

## 5. 前端组件体系

### 5.1 路由设计

```
/                      → GraphPage         # 知识图谱主页（默认）
/graph/:domain         → GraphPage         # 按领域过滤图谱
/knowledge/:id         → KnowledgeDetail   # 知识点详情
/search?q=xxx          → SearchPage        # 搜索
/exam                  → ExamPage          # 考试（Phase 3）
/exam/:id              → ExamDetail        # 具体考试
/admin                 → AdminDashboard    # 管理后台
/admin/knowledge       → KnowledgeList     # 知识点管理
/admin/knowledge/edit/:id → KnowledgeEdit  # 编辑
```

### 5.2 组件树

```
<App>
  └── <MainLayout>
      ├── <Header>                    # 顶部：Logo + 搜索框 + 用户
      ├── <Sidebar>                   # 侧边：七大领域导航 + 层级切换
      ├── <Breadcrumb>                # 面包屑导航
      └── <Content>                   # Router outlet
          │
          ├── <GraphPage>
          │   ├── <GraphControls>     # 控制栏：全部/按领域/按层级
          │   ├── <KnowledgeGraph>    # D3 力导向图
          │   │   ├── <svg>           # D3 渲染
          │   │   │   ├── links       # 边的线条
          │   │   │   └── nodes       # 节点（圆形，按 domain 着色）
          │   │   └── tooltip         # 悬浮提示
          │   └── <GraphLegend>       # 图例
          │
          ├── <KnowledgeDetail>
          │   ├── <VisualContainer>   # 互动演示区域（如果有）
          │   │   ├── <NumberLine>    # 数轴演示
          │   │   ├── <ComplexPlane>  # 复平面演示
          │   │   └── ...
          │   ├── <ContentSection>    # 定义/要点/示例等区块
          │   ├── <Formula>           # KaTeX 公式
          │   ├── <RelationCard>      # 前驱/后继知识卡片
          │   └── <PracticePanel>     # 随堂练习面板（Phase 3）
          │
          └── <AdminDashboard>...
```

### 5.3 核心组件技术方案

#### 5.3.1 D3 力导向图 (KnowledgeGraph.tsx)

核心实现逻辑：

```typescript
// 使用 D3 forceSimulation
const simulation = d3.forceSimulation(nodes)
  .force("link", d3.forceLink(edges).id(d => d.id).distance(150))
  .force("charge", d3.forceManyBody().strength(-400))
  .force("center", d3.forceCenter(width / 2, height / 2))
  .force("collision", d3.forceCollide().radius(30));

// 节点渲染
// - 颜色 = domain 映射
// - 大小 = difficulty 映射 (d=1小, d=5大)
// - 形状：普通节点=圆形，总结节点=菱形
// - 点击 → 跳转详情页

// 边渲染
// - 线条样式：prerequisite=实线, next=虚线, reference=点线
// - 箭头指示方向（前驱→后继）

// 交互
// - 拖拽节点
// - 滚轮缩放
// - 悬浮高亮路径
// - 双击展开/收起子图
```

#### 5.3.2 Canvas 互动演示 (VisualContainer)

```typescript
// VisualContainer 根据 config 渲染对应的演示组件
interface VisualConfig {
  type: 'canvas' | 'three' | 'svg';
  component: string;        // 组件标识，如 'complex-rotation'
  config: Record<string, any>;
}

// 注册机制
const visualRegistry = {
  'number-line': NumberLine,
  'complex-rotation': ComplexPlane,
  'function-transform': FunctionTransform,
  // ...
};

// 演示容器自动根据 visualType 和 visualConfig 加载组件
```

---

## 6. 知识图谱数据模型

### 6.1 知识点编码规则

`MATH-{领域编号}-{章节编号}[-{序号}]`

```
领域编号：
  01 = 数的世界
  02 = 式
  03 = 方程与不等式
  04 = 函数
  05 = 几何
  06 = 排列组合与概率统计
  07 = 数列与导数

章节编号（以"数的世界"为例）：
  01 = 自然数
  02 = 整数与负数
  03 = 有理数
  04 = 实数
  05 = 复数
  99 = 总结节点

示例：
  MATH-01-001      = 自然数
  MATH-01-002      = 整数与负数
  MATH-01-002-01   = 相反数（整数与负数的子知识点）
  MATH-01-005      = 复数入门
  MATH-01-005-01   = 虚数单位 i
  MATH-01-005-02   = 复数运算
  MATH-01-099      = 数的世界·全景总结
```

### 6.2 关系类型

| relation_type | 含义 | 图示 |
|-------------|------|------|
| `prerequisite` | 前置知识（学这个之前要懂） | 实线箭头 A → B |
| `next` | 后继知识（学了这个可以学什么） | 虚线箭头 A → B |
| `reference` | 参考关联（知识交叉引用） | 点线无箭头 |
| `summary_of` | 总结节点涵盖范围 | 菱形节点连线到涵盖范围 |

### 6.3 查询图谱的 SQL 逻辑

```sql
-- 全量图谱
SELECT n.id, n.title, n.domain, n.level, n.difficulty, n.milestone_type,
       r.from_node_id, r.to_node_id, r.relation_type, r.description
FROM knowledge_node n
LEFT JOIN knowledge_relation r ON n.id = r.from_node_id OR n.id = r.to_node_id
WHERE n.status = 1;

-- 某个节点的前后置
SELECT * FROM knowledge_relation
WHERE from_node_id = 'MATH-01-005' OR to_node_id = 'MATH-01-005';
```

---

## 7. "数的世界"领域内容设计

### 7.1 章节结构

```
数的世界（MATH-01） —— 全国统一编号
│
├── 01. 自然数（MATH-01-001） [初中 · ★]
│   ├── 自然数的概念
│   ├── 自然数的运算（加减乘除）
│   ├── 质数与合数
│   └── 因数与倍数
│
├── 02. 整数与负数（MATH-01-002） [初中 · ★]
│   ├── 整数的概念
│   ├── 相反数（×-1 = 反向）
│   ├── 绝对值
│   └── 正负数运算
│
├── 03. 有理数（MATH-01-003） [初中 · ★★]
│   ├── 分数与小数
│   ├── 有理数的概念
│   └── 有理数的运算律
│
├── 04. 实数（MATH-01-004） [初中→高中 · ★★]
│   ├── 平方根与立方根
│   ├── 无理数（π, √2）
│   ├── 实数集与数轴
│   └── 实数运算
│
├── 05. 复数（MATH-01-005） [高中 · ★★★]
│   ├── 虚数单位 i（i²=-1, ×i=旋转90°）
│   ├── 复数的概念（a+bi）
│   ├── 复数运算（加减乘除）
│   ├── 复数的几何意义（复平面）
│   ├── 共轭复数与模
│   └── 复数的三角形式与欧拉公式
│
└── 99. 数的世界·总结（MATH-01-099）[总结节点]
    ├── 知识全景图
    ├── 核心脉络：自然数→整数→有理数→实数→复数
    ├── 关键洞察：×1 正向 / ×(-1) 反向 / ×i 旋转
    └── 易错点汇总
```

### 7.2 "复数"章节的互动演示配置

每个需要交互演示的知识点配一个 `visualConfig`：

```json
// MATH-01-005-01 虚数单位 i
{
  "visualType": "canvas",
  "visuals": [
    {
      "type": "canvas",
      "component": "complex-rotation",
      "title": "乘以 i = 旋转 90°",
      "config": {
        "mode": "rotation-demo",
        "animateEntry": true,
        "showUnitCircle": true,
        "showAngleLabel": true,
        "showCoordinates": true,
        "steps": [
          { "label": "1 × 1 = 1 → 向右", "vector": [1, 0] },
          { "label": "1 × (-1) = -1 → 反向", "vector": [-1, 0] },
          { "label": "1 × i = i → 向上", "vector": [0, 1] },
          { "label": "i × i = -1 → 再转 90°", "vector": [-1, 0] }
        ]
      }
    }
  ]
}
```

### 7.3 种子数据文件格式

`knowledge-base/seed-data.json` 初始化的数据格式：

```json
{
  "nodes": [
    {
      "id": "MATH-01-001",
      "title": "自然数",
      "subtitle": "1, 2, 3, ... 是最早认识的数",
      "domain": "数",
      "level": "初中",
      "difficulty": 1,
      "sortOrder": 100,
      "visualType": "static",
      "summary": "自然数是用来数"有多少个"的数",
      "milestoneType": null,
      "contentJson": { "text": "自然数是从人类计数需求中产生的..." }
    }
  ],
  "relations": [
    {
      "fromNodeId": "MATH-01-001",
      "toNodeId": "MATH-01-002",
      "relationType": "next",
      "sortOrder": 1,
      "description": "自然数引入正数概念后，自然延伸到整数"
    }
  ]
}
```

---

## 8. 互动演示设计

### 8.1 数轴演示 — 正/反/旋转的核心演示

这是项目的"灵魂演示"，展示 ×1 / ×(-1) / ×i 的直观理解。

**Canvas 实现方案：**

```
┌─────────────────────────────────────────────┐
│                                              │
│  ← Control Panel →                          │
│                                              │
│  ┌──────┐  ┌──────┐  ┌──────┐              │
│  │ ×1   │  │×(-1) │  │ ×i   │              │
│  │ 正向  │  │ 反向  │  │ 旋转  │              │
│  └──────┘  └──────┘  └──────┘              │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │                                      │    │
│  │  数轴/复平面 Canvas 区域              │    │
│  │                                      │    │
│  │  ●─────────────────────────●──→      │    │
│  │  0                        1          │    │
│  │                                      │    │
│  │  Step 演示：1 → ×i → i → ×i → -1    │    │
│  │  向量箭头动画                       │    │
│  │                                      │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  【文字说明区域】                             │
│  ×1 = 保持方向不变                          │
│  ×(-1) = 旋转 180°（反向）                  │
│  ×i = 旋转 90°（打开新维度）                 │
│                                              │
└─────────────────────────────────────────────┘
```

**交互设计：**

1. 点击 ×1 → 箭头向右，标数字 1
2. 点击 ×(-1) → 箭头旋转 180° 指向左，标数字 -1，显示角度标记
3. 点击 ×i → 创建复平面坐标系，箭头旋转 90° 指向上，标数字 i
4. 连续点击 ×i → i → -1 → -i → 1（演示 i 的 4 次循环）
5. 拖拽滑块改变角度（高中复数三角形式）

### 8.2 函数变换演示

```
┌──────────────────────────────────────────────┐
│  y = 2(x-1)² + 3                            │
│                                              │
│  a: ────●──────  2.0                        │
│  h: ────●──────  1.0  (左右平移)             │
│  k: ────●──────  3.0  (上下平移)             │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │         函数图像 Canvas               │    │
│  │    y                                  │    │
│  │    ↑                                  │    │
│  │    │   ╱                              │    │
│  │    │ ╱                                │    │
│  │    ────────────→ x                    │    │
│  │                                      │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  左侧滑块控制 a/h/k，图像实时变化            │
│  按钮：显示 y=x² 原图对比                     │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 9. Phase 1 实现任务拆解

## 9. 实施状态

### Phase 1 — 骨架搭建 ✅

「数的世界」领域完整可用 + 前后端脚手架

### Phase 2 — 内容填充 + 互动演示 ✅

- 三大领域（数、式、方程与不等式）共 23 个知识点，61 条关系
- Canvas 互动演示组件注册表（VisualContainer 动态加载）
- 数轴正反方向演示、复平面旋转、函数变换参数调节、方程组交点
- Tauri v2 桌面壳搭建（Cargo 编译通过）

### Phase 3 — 训练考试系统（待启动）

```
✅ 题目管理（题库 CRUD + 多题型支持）
✅ 知识点练习（随堂练 + 章节练）
✅ 在线考试（组卷 + 计时 + 自动批改）
✅ 成绩分析 + 错题本
✅ Three.js 3D 立体几何演示
```

### Phase 4 — 几何 + 全领域覆盖（待启动）

```
✅ 几何领域全部知识点 + 3D 互动演示
✅ 函数领域的完整内容（包含三角函数、幂指对）
✅ 排列组合 + 统计概率
✅ 数列 + 导数初步
✅ 章节总结节点 + 阶段全景回顾页面
```

### Phase 5 — AI + 多端打包（待启动）

```
✅ AI 知识问答接入（大模型 API）
✅ AI 错题解析
✅ AI 学习路径推荐
✅ AI 智能组卷
✅ Tauri 桌面端完善 + 打包
✅ 启动时自动拉起 Spring Boot 后端
```

### Phase 6 — 打磨与上线（待启动）

```
✅ 性能优化 + 代码分割
✅ 域名 + 部署（腾讯云）
✅ 内容审核 + 数据安全
✅ 推广落地页
✅ 上线后持续迭代
```

## 10. 当前种子数据量

| 领域 | 知识点 | 关系 | 状态 |
|------|--------|------|------|
| ① 数 | 9 | 21 | ✅ |
| ② 式 | 8 | 21 | ✅ |
| ③ 方程与不等式 | 6 | 19 | ✅ |
| **合计** | **23** | **61** | |

## 11. Tauri 桌面端配置

### 目录结构

```
math-knowledge/
├── src-tauri/
│   ├── Cargo.toml          # Rust 依赖
│   ├── tauri.conf.json     # Tauri 配置（窗口、CSP、构建命令）
│   ├── capabilities/       # Tauri v2 权限配置
│   ├── icons/              # 应用图标
│   └── src/
│       ├── main.rs         # 入口
│       └── lib.rs          # 应用初始化
```

### 启动命令

```bash
# 开发模式（Web + 原生窗口，前端热更新）
cd web && npm run tauri:dev

# 生产打包（生成 .app / .dmg）
cd web && npm run tauri:build
```

### 技术方案

- **Dev 模式**：Tauri 启动 Vite 开发服务器（port 8082），WebView 加载 dev URL
- **Build 模式**：先 `npm run build` 生成 `web/dist/`，Tauri 打包为原生桌面应用
- **后端处理**：当前 dev 模式需手动启动 Spring Boot；生产模式可在 Rust 端通过 `std::process::Command` 启动后端 JAR

### 9.2 第一阶段种子数据量

| 内容 | 数量 |
|------|------|
| 知识点节点 | 15-20 个 |
| 知识点关系 | 25-30 条 |
| Canvas 互动演示 | 2 个（数轴 + 复数旋转） |
| 总结节点 | 1 个（数的世界·总结） |

---

## 附录

### A. 建表 DDL 快速执行

Phase 1 只需要 `knowledge_node`、`knowledge_relation`、`user` 三张表。建好直接跑 DataSeeder 填充种子数据。

### B. 后端启动流程

```bash
# 1. 启动 MySQL + Redis
cd math-knowledge/docker
docker compose up -d mysql redis

# 2. 初始化数据库
mysql -h 127.0.0.1 -u root -p < server/src/main/resources/db/init.sql

# 3. 启动后端
cd ../server
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 4. 启动前端
cd ../web
npm install
npm run dev
```

### C. 前端环境变量

```
VITE_API_BASE_URL=http://localhost:8080  # dev proxy → /api
VITE_WS_URL=ws://localhost:8080          # WebSocket（后续）
```

### D. 开发规范

- 所有 API 统一前缀 `/api`（通过 Vite proxy 转发至后端）
- 代码规范遵循阿里巴巴 Java 开发手册 + ESLint + Prettier
- 提交格式：`feat:`, `fix:`, `docs:`, `refactor:` 前缀

---

> **下一步行动：**
> 1. ✅ 确认本详细设计方案是否覆盖你的预期
> 2. 开始 Phase 1.1 项目初始化（建目录、配依赖）
> 3. 并行推进前后端基础代码
